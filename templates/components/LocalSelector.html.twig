<div{{ attributes }}>
    <form>
        <label for="locale">Langue</label>
        <select name="_locale" id="locale">
            <option value="">--</option>
            {% for locale in ['en', 'fr', 'es', 'de'] %}
                <option value="{{ locale }}">{{ locale | language_name }}</option>
            {% endfor %}
        </select>
        <button aria-label="Sélectionner">✅</button>
    </form>

    <output>
        {{ locale | language_name }}
    </output>
</div>
